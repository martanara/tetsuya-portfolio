import React from "react";

import "./styles.scss";

const Bands = () => {
    return (
        <section className="bands">
            <p>Welcome to bands</p>
        </section>
    );
};

export default Bands;